#ifndef SORT_H
#define SORT_H

#include <vector>
#include <fstream>

#include <iomanip>

#define DEBUG 0
#define DP(a) {if(DEBUG) {a} }          // makes turning print statements on
                                        // and off a bit easier
namespace
{
    typedef std::vector<int> svector;
}

#endif  // SORT_H

